const { getSupport } = require('caniuse-api');

/**
const CurrentRetro = styled.div`
  position: absolute;
  right: 10px;
  bottom: ${props => (props.simple ? '18px' : '10px')};
  height: 30px;
  width: 30px;
`;

github integration api
*/
